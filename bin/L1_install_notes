Installing a new L1 system instance:

pip install Redis
pip install Pika
Rabbit-mq if necessary, with Erlang
Python
Latest CPP, gcc, g++
pip install Yaml
pip install influxdb-python
pip install pip install lxml
CMake      yum install cmake
yaml-cpp      git clone git://github.com/jbeder/yaml-cpp
Git
libxml
libxml-devel
libxml2
libxml2-devel
libxslt
libxslt-devel

OCS DDS in /opt/sal/ and set environment confog file

Setup.py in this dir sets up rabbit queues and exchanges, but does not do users yet.

